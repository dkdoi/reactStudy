import React, { Component } from 'react';

export default class extends Component {
    render() {
        return(
        <div>
            363963
            {this.props.children}
        </div>
        )
    }
}